{
  "name": "serverstick-cloud",
  "version": "0.1.0",
  "description": "ServerStick cloud API — model discovery, key validation, bootstrap serving",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "^41.0.0",
    "vitest": "^3.0.0"
  }
}